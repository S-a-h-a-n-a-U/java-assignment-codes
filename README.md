#Question

Technical training - Core Java 
Note: Implement exception handling in all 
1. Write a program to add 2 numbers. (Note: consider all the cases) 
2. Write a program to check if a given list of integers contains only odd or even numbers 
3. Write a program to check whether two strings are anagram or not? 
4. Write a program to implement binary search 
5. Write a program to print the repeated letters and its occurrence in a given string (letters that occur only once will not be printed. 
6. Create an interface with a method to swap numbers. Implement this method in 2 diff classes. In 1 class swap numbers using temp variable and in another without using temp variable. 
7. Find the highest element in an array which is greater than its Neighbours. 
8. Find the subarray in array which will add up to the given sum. 
9. Given two sorted arrays in increasing order. Merge them in sorted order without using any extra space. Modify the first array so that it contains the first N elements and modify the second array so that it contains the last M elements where N and M are the length of first and second array 
